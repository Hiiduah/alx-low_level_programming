#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry
 * Return: 0 (Success)
 */
int main(void)
{
	int d;
	char low;

	for (d = '0'; d <= 'f'; low++)
	putchar(low);
	putchar('\n');

	 return (0);
}
